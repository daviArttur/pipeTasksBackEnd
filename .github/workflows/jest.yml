name: Jest

on: [ push ]

jobs:
  testingLibrary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - run: npm i
      - run: npm run dev & 
      - run: npm run test